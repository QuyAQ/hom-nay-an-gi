import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFF6B35);    // Cam - màu ẩm thực
  static const Color secondary = Color(0xFF2ECC71);  // Xanh lá - healthy
  static const Color accent = Color(0xFFF39C12);     // Vàng
  static const Color background = Color(0xFFFEF9F0); // Kem nhạt
  static const Color cardBackground = Colors.white;
  static const Color textPrimary = Color(0xFF2C3E50);
  static const Color textSecondary = Color(0xFF7F8C8D);
  static const Color error = Color(0xFFE74C3C);
}